/** Minimal oracledb stub — keeps lib/auth.js importable without a real DB. */
export const OUT_FORMAT_OBJECT = 4002;
export const BLOB = 2019;
export const BIND_IN = 3001;
export default { OUT_FORMAT_OBJECT, BLOB, BIND_IN };
